- name: Apply User configuration
  hosts: target
  become: yes

  vars_files:
    - ../vars/users.yml
    - ../vars/user_roles.yml

  tasks:

    - name: Ensure existence of groups
      ansible.builtin.group:
        name: "{{ item }}"
        state: present
      loop: >-
        {{
          users
          | map(attribute='role')
          | map('extract', roles)
          | map(attribute='groups')
          | flatten
          | unique
        }}

    - name: Create Users
      ansible.builtin.user:
        name: "{{ item.name }}"
        groups: "{{ roles[item.role].groups | join(',') }}"
        append: yes
        password_lock: true
        shell: "{{ item.shell }}"
        state: "{{ item.state }}"
      loop: "{{ users }}"
    - name: Ensure files directory exists
      ansible.builtin.file:
        path: "{{ playbook_dir }}/../files"
        state: directory
      delegate_to: localhost
      run_once: true

    - name: Ensure SSH Directories exist
      ansible.builtin.file:
        path: "/home/{{ item.name }}/.ssh"
        state: directory
        mode: '0700'
        owner: "{{ item.name }}"
        group: "{{ item.name }}"
      when: item.state == "present"
      loop: "{{ users }}"

    - name: Create SSH Key pair for users (control node)
      ansible.builtin.openssh_keypair:
        path: "{{ playbook_dir }}/../files/{{ item.name }}"
        type: ed25519
        mode: '0600'
      delegate_to: localhost
      run_once: false
      when: item.state == "present"
      loop: "{{ users }}"

    - name: Add SSH key to authorized Key
      ansible.posix.authorized_key:
        user: "{{ item.name }}"
        key: "{{ lookup('file', playbook_dir + '/../files/' + item.name + '.pub') }}"
        state: present
      when: item.state == "present"
      loop: "{{ users }}"

    - name: Setup sudo access
      ansible.builtin.copy:
        dest: "/etc/sudoers.d/{{ item.name }}"
        content: "{{ item.name }} {{ roles[item.role].sudo | trim }}"
        mode: '0440'
        validate: '/usr/sbin/visudo -cf %s'
      when:
        - item.state == "present"
        - roles[item.role].sudo | length > 0
      loop: "{{ users }}"
