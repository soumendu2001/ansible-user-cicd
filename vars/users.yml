
users:
  - name: sudip
    group_name: administrator
    state: present
    shell: /bin/bash
    
  - name: soumyendu
    group_name: webadmin
    state: present
    shell: /bin/bash
    
  - name: alice
    group_name: webadmin
    state: present
    shell: /bin/bash
    

  - name: bob
    group_name: dbadmin
    state: present
    shell: /bin/bash
    

  - name: charlie
    group_name: developers
    state: present
    .github/workflows/user_mgmt.yml]

             

      - name: Install Ansible
        run: |
          sudo apt update
          sudo apt install -y ansible

      - name: Setup ssh key 
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa

      - name: Run playbook
        run: |
          ansible-playbook -i inventory playbooks/user_management.yml
      
playbooks/user_management.yml

- name: Apply User configuration
  hosts: target
  become: yes

  vars_files:
    - ../vars/users.yml
    - ../vars/user_roles.yml

  tasks:

    - name: Ensure existence of groups
      ansible.builtin.group:
        name: "{{ item }}"
        state: present
      loop: >-
        {{
          users
          | map(attribute='group_name')
          | map('extract', user_roles)
          | map(attribute='groups')
          | flatten
          | unique
        }}

    - name: Create Users
      ansible.builtin.user:
        name: "{{ item.name }}"
        groups: "{{ user_roles[item.group_name].groups | join(',') }}"
        append: yes
        create_home: yes
        password_lock: true
        shell: "{{ item.shell }}"
        state: "{{ item.state }}"
      loop: "{{ users }}"
    - name: Ensure files directory exists
      ansible.builtin.file:
        path: "{{ playbook_dir }}/../files"
        state: directory
      delegate_to: localhost
      run_once: true

    - name: Ensure SSH Directories exist
      ansible.builtin.file:
        path: "/home/{{ item.name }}/.ssh"
        state: directory
        mode: '0700'
        owner: "{{ item.name }}"
        group: "{{ item.name }}"
      when: item.state == "present"
      loop: "{{ users }}"

    - name: Generate SSH Key pair (idempotent)
      community.crypto.openssh_keypair:
         path: "{{ playbook_dir }}/../files/{{ item.name }}"
         type: ed25519
         mode: '0600'
         regenerate: full_idempotence
      delegate_to: localhost
      register: ssh_keys
      loop: "{{ users }}"
      when: item.state == "present"

    - name: Set authorized key (single source of truth)
      ansible.posix.authorized_key:
         user: "{{ item.item.name }}"
         key: "{{ item.public_key }}"
         exclusive: true
      loop: "{{ ssh_keys.results }}"
      when: item.public_key is defined


    - name: Setup sudo access
      ansible.builtin.copy:
        dest: "/etc/sudoers.d/{{ item.key }}"
        content: "%{{ item.key }} {{ item.value.sudo }}\n"
        mode: '0440'
        validate: '/usr/sbin/visudo -cf %s'
      when:
        - item.value.sudo | length > 0
      loop: "{{ user_roles | dict2items }}"

    - name: Fast sudo validation
      shell: |
        {% for user in users if user.state == "present" and user_roles[user.group_name].sudo|length > 0 %}
         echo "Testing {{ user.name }}"
        sudo -u {{ user.name }} sudo whoami
        {% endfor %}


vars/user_management.yml

- name: Apply User configuration
  hosts: target
  become: yes

  vars_files:
    - ../vars/users.yml
    - ../vars/user_roles.yml

  tasks:

    - name: Ensure existence of groups
      ansible.builtin.group:
        name: "{{ item }}"
        state: present
      loop: >-
        {{
          users
          | map(attribute='group_name')
          | map('extract', user_roles)
          | map(attribute='groups')
          | flatten
          | unique
        }}

    - name: Create Users
      ansible.builtin.user:
        name: "{{ item.name }}"
        groups: "{{ user_roles[item.group_name].groups | join(',') }}"
        append: yes
        password_lock: true
        shell: "{{ item.shell }}"
        state: "{{ item.state }}"
      loop: "{{ users }}"
    - name: Ensure files directory exists
      ansible.builtin.file:
        path: "{{ playbook_dir }}/../files"
        state: directory
      delegate_to: localhost
      run_once: true

    - name: Ensure SSH Directories exist
      ansible.builtin.file:
        path: "/home/{{ item.name }}/.ssh"
        state: directory
        mode: '0700'
        owner: "{{ item.name }}"
        group: "{{ item.name }}"
      when: item.state == "present"
      loop: "{{ users }}"

    - name: Generate SSH Key pair for users (control node)
      ansible.builtin.openssh_keypair:
        path: "{{ playbook_dir }}/../files/{{ item.name }}"
        type: ed25519
        mode: '0600'
      register: ssh_keys
      delegate_to: localhost
      run_once: false
      when: item.state == "present"
      loop: "{{ users }}"

    - name: Add SSH key to authorized Key
      ansible.posix.authorized_key:
        user: "{{ item.item.name }}"
        key: "{{ item.public_key }}"
        state: present
      loop: "{{ ssh_keys.results}}"
      when: item.item.state == "present"
      

    - name: Setup sudo access
      ansible.builtin.copy:
        dest: "/etc/sudoers.d/{{ item.name }}"
        content: "{{ item.name }} {{ user_roles[item.group_name].sudo | trim }}\n"
        mode: '0440'
        validate: '/usr/sbin/visudo -cf %s'
      when:
        - item.state == "present"
        - user_roles[item.group_name].sudo | length > 0
      loop: "{{ users }}"


vars/user_roles.yml

user_roles:
  SIAM:
     groups: ["SIAM"]
     sudo: ""
  administrator:
    groups: ["administrator"]
    sudo: "ALL=(ALL) NOPASSWD:ALL"
  webadmin:
    groups: ["webadmin"]
    sudo: "ALL=(ALL) NOPASSWD:ALL"

  dbadmin:
    groups: ["dbadmin"]
    sudo: "ALL=(ALL) ALL"

  readonly:
    groups: ["readonly"]
    sudo: ""

  developers:
     groups: ["developers"]
     sudo: ""
   
  management:
     groups: ["management"]
     sudo: ""

vars/users.yml

users:
  - name: sudip
    group_name: administrator
    state: present
    shell: /bin/bash
    ssh_key: sudip.pub
  - name: soumyendu
    group_name: webadmin
    state: present
    shell: /bin/bash
    ssh_key: soumyendu.pub
  - name: alice
    group_name: webadmin
    state: present
    shell: /bin/bash
    ssh_key: alice.pub

  - name: bob
    group_name: dbadmin
    state: present
    shell: /bin/bash
    ssh_key: bob.pub

  - name: charlie
    group_name: developers
    state: present
    shell: /bin/bash
    ssh_key: charlie.pub

  - name: julia
    group_name: SIAM
    state: present
    shell: /bin/bash
    .github/workflows/user_mgmt.yml]

name: Ansible User CI/CD

on:
  push: 
    paths: 
      - 'playbooks/**'
      - 'vars/**'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
             

      - name: Install Ansible
        run: |
          sudo apt update
          sudo apt install -y ansible

      - name: Setup ssh key 
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa

      - name: Run playbook
        run: |
          ansible-playbook -i inventory playbooks/user_management.yml
      
playbooks/user_management.yml

- name: Apply User configuration
  hosts: target
  become: yes

  vars_files:
    - ../vars/users.yml
    - ../vars/user_roles.yml

  tasks:

    - name: Ensure existence of groups
      ansible.builtin.group:
        name: "{{ item }}"
        state: present
      loop: >-
        {{
          users
          | map(attribute='group_name')
          | map('extract', user_roles)
          | map(attribute='groups')
          | flatten
          | unique
        }}

    - name: Create Users
      ansible.builtin.user:
        name: "{{ item.name }}"
        groups: "{{ user_roles[item.group_name].groups | join(',') }}"
        append: yes
        create_home: yes
        password_lock: true
        shell: "{{ item.shell }}"
        state: "{{ item.state }}"
      loop: "{{ users }}"
    - name: Ensure files directory exists
      ansible.builtin.file:
        path: "{{ playbook_dir }}/../files"
        state: directory
      delegate_to: localhost
      run_once: true

    - name: Ensure SSH Directories exist
      ansible.builtin.file:
        path: "/home/{{ item.name }}/.ssh"
        state: directory
        mode: '0700'
        owner: "{{ item.name }}"
        group: "{{ item.name }}"
      when: item.state == "present"
      loop: "{{ users }}"

    - name: Generate SSH Key pair (idempotent)
      community.crypto.openssh_keypair:
         path: "{{ playbook_dir }}/../files/{{ item.name }}"
         type: ed25519
         mode: '0600'
         regenerate: full_idempotence
      delegate_to: localhost
      register: ssh_keys
      loop: "{{ users }}"
      when: item.state == "present"

    - name: Set authorized key (single source of truth)
      ansible.posix.authorized_key:
         user: "{{ item.item.name }}"
         key: "{{ item.public_key }}"
         exclusive: true
      loop: "{{ ssh_keys.results }}"
      when: item.public_key is defined


    - name: Setup sudo access
      ansible.builtin.copy:
        dest: "/etc/sudoers.d/{{ item.key }}"
        content: "%{{ item.key }} {{ item.value.sudo }}\n"
        mode: '0440'
        validate: '/usr/sbin/visudo -cf %s'
      when:
        - item.value.sudo | length > 0
      loop: "{{ user_roles | dict2items }}"

    - name: Fast sudo validation
      shell: |
        {% for user in users if user.state == "present" and user_roles[user.group_name].sudo|length > 0 %}
         echo "Testing {{ user.name }}"
        sudo -u {{ user.name }} sudo whoami
        {% endfor %}


vars/user_management.yml

- name: Apply User configuration
  hosts: target
  become: yes

  vars_files:
    - ../vars/users.yml
    - ../vars/user_roles.yml

  tasks:

    - name: Ensure existence of groups
      ansible.builtin.group:
        name: "{{ item }}"
        state: present
      loop: >-
        {{
          users
          | map(attribute='group_name')
          | map('extract', user_roles)
          | map(attribute='groups')
          | flatten
          | unique
        }}

    - name: Create Users
      ansible.builtin.user:
        name: "{{ item.name }}"
        groups: "{{ user_roles[item.group_name].groups | join(',') }}"
        append: yes
        password_lock: true
        shell: "{{ item.shell }}"
        state: "{{ item.state }}"
      loop: "{{ users }}"
    - name: Ensure files directory exists
      ansible.builtin.file:
        path: "{{ playbook_dir }}/../files"
        state: directory
      delegate_to: localhost
      run_once: true

    - name: Ensure SSH Directories exist
      ansible.builtin.file:
        path: "/home/{{ item.name }}/.ssh"
        state: directory
        mode: '0700'
        owner: "{{ item.name }}"
        group: "{{ item.name }}"
      when: item.state == "present"
      loop: "{{ users }}"

    - name: Generate SSH Key pair for users (control node)
      ansible.builtin.openssh_keypair:
        path: "{{ playbook_dir }}/../files/{{ item.name }}"
        type: ed25519
        mode: '0600'
      register: ssh_keys
      delegate_to: localhost
      run_once: false
      when: item.state == "present"
      loop: "{{ users }}"

    - name: Add SSH key to authorized Key
      ansible.posix.authorized_key:
        user: "{{ item.item.name }}"
        key: "{{ item.public_key }}"
        state: present
      loop: "{{ ssh_keys.results}}"
      when: item.item.state == "present"
      

    - name: Setup sudo access
      ansible.builtin.copy:
        dest: "/etc/sudoers.d/{{ item.name }}"
        content: "{{ item.name }} {{ user_roles[item.group_name].sudo | trim }}\n"
        mode: '0440'
        validate: '/usr/sbin/visudo -cf %s'
      when:
        - item.state == "present"
        - user_roles[item.group_name].sudo | length > 0
      loop: "{{ users }}"


vars/user_roles.yml

user_roles:
  SIAM:
     groups: ["SIAM"]
     sudo: ""
  administrator:
    groups: ["administrator"]
    sudo: "ALL=(ALL) NOPASSWD:ALL"
  webadmin:
    groups: ["webadmin"]
    sudo: "ALL=(ALL) NOPASSWD:ALL"

  dbadmin:
    groups: ["dbadmin"]
    sudo: "ALL=(ALL) ALL"

  readonly:
    groups: ["readonly"]
    sudo: ""

  developers:
     groups: ["developers"]
     sudo: ""
   
  management:
     groups: ["management"]
     sudo: ""

vars/users.yml

users:
  - name: sudip
    group_name: administrator
    state: present
    shell: /bin/bash
    .github/workflows/user_mgmt.yml]

name: Ansible User CI/CD

on:
  push: 
    paths: 
      - 'playbooks/**'
      - 'vars/**'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
             

      - name: Install Ansible
        run: |
          sudo apt update
          sudo apt install -y ansible

      - name: Setup ssh key 
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa

      - name: Run playbook
        run: |
          ansible-playbook -i inventory playbooks/user_management.yml
      
playbooks/user_management.yml

- name: Apply User configuration
  hosts: target
  become: yes

  vars_files:
    - ../vars/users.yml
    - ../vars/user_roles.yml

  tasks:

    - name: Ensure existence of groups
      ansible.builtin.group:
        name: "{{ item }}"
        state: present
      loop: >-
        {{
          users
          | map(attribute='group_name')
          | map('extract', user_roles)
          | map(attribute='groups')
          | flatten
          | unique
        }}

    - name: Create Users
      ansible.builtin.user:
        name: "{{ item.name }}"
        groups: "{{ user_roles[item.group_name].groups | join(',') }}"
        append: yes
        create_home: yes
        password_lock: true
        shell: "{{ item.shell }}"
        state: "{{ item.state }}"
      loop: "{{ users }}"
    - name: Ensure files directory exists
      ansible.builtin.file:
        path: "{{ playbook_dir }}/../files"
        state: directory
      delegate_to: localhost
      run_once: true

    - name: Ensure SSH Directories exist
      ansible.builtin.file:
        path: "/home/{{ item.name }}/.ssh"
        state: directory
        mode: '0700'
        owner: "{{ item.name }}"
        group: "{{ item.name }}"
      when: item.state == "present"
      loop: "{{ users }}"

    - name: Generate SSH Key pair (idempotent)
      community.crypto.openssh_keypair:
         path: "{{ playbook_dir }}/../files/{{ item.name }}"
         type: ed25519
         mode: '0600'
         regenerate: full_idempotence
      delegate_to: localhost
      register: ssh_keys
      loop: "{{ users }}"
      when: item.state == "present"

    - name: Set authorized key (single source of truth)
      ansible.posix.authorized_key:
         user: "{{ item.item.name }}"
         key: "{{ item.public_key }}"
         exclusive: true
      loop: "{{ ssh_keys.results }}"
      when: item.public_key is defined


    - name: Setup sudo access
      ansible.builtin.copy:
        dest: "/etc/sudoers.d/{{ item.key }}"
        content: "%{{ item.key }} {{ item.value.sudo }}\n"
        mode: '0440'
        validate: '/usr/sbin/visudo -cf %s'
      when:
        - item.value.sudo | length > 0
      loop: "{{ user_roles | dict2items }}"

    - name: Fast sudo validation
      shell: |
        {% for user in users if user.state == "present" and user_roles[user.group_name].sudo|length > 0 %}
         echo "Testing {{ user.name }}"
        sudo -u {{ user.name }} sudo whoami
        {% endfor %}


vars/user_management.yml

- name: Apply User configuration
  hosts: target
  become: yes

  vars_files:
    - ../vars/users.yml
    - ../vars/user_roles.yml

  tasks:

    - name: Ensure existence of groups
      ansible.builtin.group:
        name: "{{ item }}"
        state: present
      loop: >-
        {{
          users
          | map(attribute='group_name')
          | map('extract', user_roles)
          | map(attribute='groups')
          | flatten
          | unique
        }}

    - name: Create Users
      ansible.builtin.user:
        name: "{{ item.name }}"
        groups: "{{ user_roles[item.group_name].groups | join(',') }}"
        append: yes
        password_lock: true
        shell: "{{ item.shell }}"
        state: "{{ item.state }}"
      loop: "{{ users }}"
    - name: Ensure files directory exists
      ansible.builtin.file:
        path: "{{ playbook_dir }}/../files"
        state: directory
      delegate_to: localhost
      run_once: true

    - name: Ensure SSH Directories exist
      ansible.builtin.file:
        path: "/home/{{ item.name }}/.ssh"
        state: directory
        mode: '0700'
        owner: "{{ item.name }}"
        group: "{{ item.name }}"
      when: item.state == "present"
      loop: "{{ users }}"

    - name: Generate SSH Key pair for users (control node)
      ansible.builtin.openssh_keypair:
        path: "{{ playbook_dir }}/../files/{{ item.name }}"
        type: ed25519
        mode: '0600'
      register: ssh_keys
      delegate_to: localhost
      run_once: false
      when: item.state == "present"
      loop: "{{ users }}"

    - name: Add SSH key to authorized Key
      ansible.posix.authorized_key:
        user: "{{ item.item.name }}"
        key: "{{ item.public_key }}"
        state: present
      loop: "{{ ssh_keys.results}}"
      when: item.item.state == "present"
      

    - name: Setup sudo access
      ansible.builtin.copy:
        dest: "/etc/sudoers.d/{{ item.name }}"
        content: "{{ item.name }} {{ user_roles[item.group_name].sudo | trim }}\n"
        mode: '0440'
        validate: '/usr/sbin/visudo -cf %s'
      when:
        - item.state == "present"
        - user_roles[item.group_name].sudo | length > 0
      loop: "{{ users }}"


vars/user_roles.yml

user_roles:
  SIAM:
     groups: ["SIAM"]
     sudo: ""
  administrator:
    groups: ["administrator"]
    sudo: "ALL=(ALL) NOPASSWD:ALL"
  webadmin:
    groups: ["webadmin"]
    sudo: "ALL=(ALL) NOPASSWD:ALL"

  dbadmin:
    groups: ["dbadmin"]
    sudo: "ALL=(ALL) ALL"

  readonly:
    groups: ["readonly"]
    sudo: ""

  developers:
     groups: ["developers"]
     sudo: ""
   
  management:
     groups: ["management"]
     sudo: ""

vars/users.yml

users:
  - name: sudip
    group_name: administrator
    state: present
    shell: /bin/bash
    ssh_key: sudip.pub
  - name: soumyendu
    group_name: webadmin
    state: present
    shell: /bin/bash
    ssh_key: soumyendu.pub
  - name: alice
    group_name: webadmin
    state: present
    shell: /bin/bash
    ssh_key: alice.pub

  - name: bob
    group_name: dbadmin
    state: present
    shell: /bin/bash
    ssh_key: bob.pub

  - name: charlie
    group_name: developers
    state: present
    shell: /bin/bash
    

  - name: julia
    group_name: SIAM
    state: present
    shell: /bin/bash
    


  - name: sam
    group_name: management
    state: present
    shell: /bin/bash
    

  - name: max
    group_name: readonly
    state: present
    shell: /bin/bash
    

  - name: David
    group_name: management
    state: present
    shell: /bin/bash
    


  - name: soumyendu
    group_name: webadmin
    state: present
    shell: /bin/bash
    
  - name: alice
    group_name: webadmin
    state: present
    shell: /bin/bash
    

  - name: bob
    group_name: dbadmin
    state: present
    shell: /bin/bash
    

  - name: charlie
    group_name: developers
    state: present
    shell: /bin/bash
    

  - name: julia
    group_name: SIAM
    state: present
    shell: /bin/bash
    


  - name: sam
    group_name: management
    state: present
    shell: /bin/bash
    

  - name: max
    group_name: readonly
    state: present
    shell: /bin/bash
    ssh_key: max.pub

  - name: David
    group_name: management
    state: present
    shell: /bin/bash
    

  - name: sam
    group_name: management
    state: present
    shell: /bin/bash
    

  - name: max
    group_name: readonly
    state: present
    shell: /bin/bash
    

  - name: David
    group_name: management
    state: present
    shell: /bin/bash
  - name: julia
    group_name: SIAM
    state: present
    shell: /bin/bash
    


  - name: sam
    group_name: management
    state: present
    shell: /bin/bash
    

  - name: max
    group_name: readonly
    state: present
    shell: /bin/bash
    

  - name: David
    group_name: management
    state: present
    shell: /bin/bash
    


